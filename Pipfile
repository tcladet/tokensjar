[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
toposort= "==1.10"

[dev-packages]
build = "==1.2.1"
twine = "==5.1.0"
ddt = "==1.7.2"

[requires]
python_version = "3.8"

[scripts]
test = "python -m unittest -v"
dist = "python -m build"
code = "code ."